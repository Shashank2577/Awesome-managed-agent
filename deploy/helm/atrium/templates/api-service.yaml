apiVersion: v1
kind: Service
metadata:
  name: {{ include "atrium.fullname" . }}-api
  labels:
    app: atrium-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: atrium-api
