apiVersion: v1
kind: Secret
metadata:
  name: {{ include "atrium.fullname" . }}-secrets
type: Opaque
data:
  # Base64 encode these externally or use sealed-secrets in prod
  DATABASE_URL: {{ .Values.database.url | b64enc | quote }}
